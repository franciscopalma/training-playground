pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}

    stages('build'){
		steps{
			script{
				sh 'echo date > ficheiro.txt'
				for(int i = 0; 1<2; i++){
					sh 'cat ficheiro.txt'
					sleep 10
				}
			}
			

		}
    }
}