pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
		stage('build'){
			steps{
				sh 'date > ficheiro.txt'
			}
		}
		stage('test'){
			steps{
				sh 'grep 2019 ficheiro.txt || true'
			}
		}
    }
}