pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh "curl www.google.com > file.txt"
            }
            post {
                always {
                    archiveArtifacts artifacts: 'file.txt', fingerprint: true
                }
            }
        }
    }
}