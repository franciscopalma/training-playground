pipeline {
    agent { 
		docker { 
			image 'maven:latest' 
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn -B '02 - Java Unit Tests/'"
            }
        }
    }
}