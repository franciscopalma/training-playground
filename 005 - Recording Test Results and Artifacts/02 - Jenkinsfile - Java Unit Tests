pipeline {
    agent { 
		docker { 
			image 'maven:latest' 
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn -f '../02 - Java Unit Tests/'"
            }
        }
    }
}