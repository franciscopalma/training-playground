pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    enviroment{
        NOTIFICATION_EMAIL = 'Email'
    }
    stages {
        stage('build') {
            steps {
                echo "${NOTIFICATION_EMAIL}"
            }
        }
    }
}